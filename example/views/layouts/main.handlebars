<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>PozitionJs example</title>
    <style>
      body {
          padding: 30px;
      }

      .box {
        background-color: black;
        color: white;
        position: absolute;
      }

      .content {
        background-color: white;
        color: black;
        position: absolute;
      }

      .container {
        border: 1px solid black;
        position: relative;
        background-color: lightgray;
      }
    </style>
</head>
<body>
    <h1>PozitionJs example {{exampleIndex}}</h1>
    <h2>{{description}}</h2>
    {{{body}}}
</body>
</html>
